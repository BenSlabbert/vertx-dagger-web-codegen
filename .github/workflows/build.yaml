---
name: "Java + Maven"
"on":
  push:
    branches:
    - "main"
  pull_request:
    branches:
    - "main"
jobs:
  build:
    runs-on: "ubuntu-latest"
    env:
      GH_TOKEN: "${{secrets.GH_TOKEN}}"
      TESTCONTAINERS_RYUK_DISABLED: true
    timeout-minutes: 360
    permissions:
      actions: "read"
      contents: "read"
      security-events: "write"
    strategy:
      fail-fast: false
      matrix:
        language:
        - "java"
        java-version:
        - "25"
        distribution:
        - "graalvm"
    steps:
    - name: "Install Podman"
      uses: "redhat-actions/podman-install@main"
    - name: "Set DOCKER_HOST to rootless Podman socket"
      run: "echo \"DOCKER_HOST=unix:///run/$(id -u)/podman/podman.sock\" >> $GITHUB_ENV"
    - run: "echo \"$DOCKER_HOST\""
    - uses: "actions/checkout@v6"
    - name: "Set up JDK ${{matrix.java-version}}-${{matrix.distribution}}"
      uses: "actions/setup-java@v5"
      with:
        distribution: "${{matrix.distribution}}"
        java-version: "${{matrix.java-version}}"
        java-package: "jdk"
        cache: "maven"
    - name: "Set up Maven"
      uses: "stCarolas/setup-maven@v5"
      with:
        maven-version: "3.9.14"
    - name: "Initialize CodeQL"
      uses: "github/codeql-action/init@v4"
      with:
        languages: "${{matrix.language}}"
    - run: "podman version"
    - run: "mvn -version"
    - run: "mvn -s settings.xml spotless:check -Djava.version=${{matrix.java-version}}"
    - run: "mvn -s settings.xml clean install -Djava.version=${{matrix.java-version}}"
    - name: "gradle"
      run: "./gradlew build --console=plain --no-daemon"
    - name: "Perform CodeQL Analysis"
      uses: "github/codeql-action/analyze@v4"
    - name: "Submit Dependency Snapshot"
      uses: "advanced-security/maven-dependency-submission-action@v5"
      with:
        token: "${{secrets.GH_TOKEN}}"
        maven-args: "-s settings.xml"
